<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%- include('partials/head') %>
    <title>Image Profile</title>
</head>
<body>
    <div class="container p-4">
        <div class="row">
            <div class="col-md-4 offset-md-4">
                <div class="card animate__fadeInDown">
                    <img src="<%= image.path %>" class="card-img-top" alt="...">
                    <div class="card-body">
                        <h5 class="card-title"><%= image.title %></h5>
                        <p class="card-text"><%= image.description %></p>
                        <a href="/image/delete/<%= image._id %>" class="btn btn-danger btn-block">Delete Image</a>
                    </div>
                    <div class="card-footer">
                        <small class="text-muted"><%= format(image.created_at) %></small>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>